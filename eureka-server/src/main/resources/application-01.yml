server:
  port: 8801

spring:
  application:
    name: eurekaServer

eureka:
  instance:
    hostname: eureka01
  client:
    fetch-registry: false
    #    register-with-eureka: false
    service-url:
      defaultZone: http://eureka02:8802/eureka
 